/**
 * Legacy hidden test cases for challenges not yet migrated to testCaseSpec.
 * Migrated challenges live in challenge-test-specs.ts — do not duplicate here.
 *
 * Challenges 23–28 previously had stale entries that no longer matched their
 * ownership-focused content; those were removed. Add entries here only when a
 * challenge needs legacy tests and has no testCaseSpec yet.
 */
export const CHALLENGE_TEST_CASES: Record<number, string> = {};